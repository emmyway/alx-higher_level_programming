#!/usr/bin/python3
''' script finds the peak in an array
'''


def find_peak(list_of_integers):
    '''
    finds a peak in a list of unsorted integers
    Args:
        list_of_integers (lidt): A list of unsorted integers
    Returns:
        int: the peak element jn the list.
    '''
    # validate list
    if not list_of_integers

